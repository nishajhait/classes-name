function weekDays(week) {
    console.log(week == 1 ? "MONDAY" : week == 2 ? "TUESDAY" : week == 3 ? "WEDNESDAY" : week == 4 ? "THURSDAY" : week == 5 ? "FRIDAY" : week == 6 ? "SATARDAY" : week == 7 ? "sunday" : "invalid")
}
weekDays(100)

